.text

main: 	# Salvando Caractere
	addi $4, $0, 'H'

	# Serviço de impressão de caractere
	addi $2, $0, 11
	syscall
	
	# Salvando Caractere
	addi $4, $0, 'e'

	# Serviço de impressão de caractere
	addi $2, $0, 11
	syscall
	
	# Salvando Caractere
	addi $4, $0, 'l'

	# Serviço de impressão de caractere
	addi $2, $0, 11
	syscall
	
	# Serviço de impressão de caractere
	addi $2, $0, 11
	syscall
	
	# Salvando Caractere
	addi $4, $0, 'o'

	# Serviço de impressão de caractere
	addi $2, $0, 11
	syscall
	
	# Salvando Caractere
	addi $4, $0, ' '

	# Serviço de impressão de caractere
	addi $2, $0, 11
	syscall
	
	# Salvando Caractere
	addi $4, $0, 'W'

	# Serviço de impressão de caractere
	addi $2, $0, 11
	syscall
	
	# Salvando Caractere
	addi $4, $0, 'o'

	# Serviço de impressão de caractere
	addi $2, $0, 11
	syscall
	
	# Salvando Caractere
	addi $4, $0, 'r'

	# Serviço de impressão de caractere
	addi $2, $0, 11
	syscall
	
	# Salvando Caractere
	addi $4, $0, 'l'

	# Serviço de impressão de caractere
	addi $2, $0, 11
	syscall
	
	# Salvando Caractere
	addi $4, $0, 'd'

	# Serviço de impressão de caractere
	addi $2, $0, 11
	syscall
	
	# Serviço de dinalização
	addi $2, $0, 10
	syscall